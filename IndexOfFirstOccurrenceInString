class Solution {
    public int strStr(String s, String needle) {
        for(int i=0;i<s.length();i++) {
            for(int j=i+1;j<=s.length();j++) {
                if(s.substring(i,j).equals(needle)){
                    return i;
                }
            }
		}
        return -1;
    }
}
