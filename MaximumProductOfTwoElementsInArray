class Solution {
    public int maxProduct(int[] nums) {
        Arrays.sort(nums);
        int prod1=(nums[nums.length-1]-1)*(nums[nums.length-2]-1);
        int prod2=(nums[0]-1)*(nums[1]-1);
        int ans= Math.max(prod1,prod2);
        return ans;
    }
}
